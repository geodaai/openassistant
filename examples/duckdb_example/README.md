# DuckDB Example

A minimal React + Vite app demonstrating how to render the `Assistant` component from `@openassistant/assistant` with DuckDB tools.

## Run

```bash
# from repo root
cd examples/duckdb_example
yarn install # or pnpm/npm
yarn dev
```

Then open http://localhost:5173.

You can customize the AI provider and model in `src/App.tsx` via `AssistantOptions`.
